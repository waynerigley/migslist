<nav class="navbar">
  <div class="nav-brand">
    <a href="<%= isSuperAdmin ? '/admin' : '/dashboard' %>">MIGS List</a>
    <span style="color: #32CD32; font-size: 0.7rem; margin-left: 0.5rem;">v1.2.1</span>
  </div>

  <div class="nav-links">
    <% if (isSuperAdmin) { %>
      <a href="/admin">Dashboard</a>
      <a href="/admin/signups">Signups</a>
      <a href="/admin/trials">Trials</a>
      <a href="/admin/unions">Unions</a>
      <a href="/admin/users">Users</a>
      <a href="/admin/finance">Finance</a>
    <% } else { %>
      <a href="/dashboard">Dashboard</a>
      <a href="/dashboard/search">Search</a>
    <% } %>
  </div>

  <div class="nav-user">
    <span class="user-info">
      <%= user.firstName || user.email %>
      <% if (!isSuperAdmin && user.unionName) { %>
        <small>(<%= user.unionName %>)</small>
      <% } %>
    </span>
    <button class="theme-toggle" onclick="toggleTheme()" title="Toggle theme">
      <svg class="moon-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z"/>
      </svg>
      <svg class="sun-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z"/>
      </svg>
    </button>
    <a href="/logout" class="btn btn-outline btn-sm">Logout</a>
  </div>
</nav>
